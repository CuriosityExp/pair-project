<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('css') %>
    <title>Edit Profile Form</title>
</head>
<body>
    <%- include('navbarProfile') %>
    <div class="container">
        <div class="row">
            <h1>Edit Profile Form</h1>
        </div>
        <form action="/profile/<%= user.id %>/edit" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="Fullname" class="form-label">Fullname</label>
                <input name="fullname" type="text" class="form-control" id="Fullname" value="<%= user.fullname %>" required>
            </div>
            <div class="mb-3">
                <label for="avatar" class="form-label">Avatar</label>
                <input name="avatar" type="file" class="form-control" id="avatar">
            </div>
            <div class="mb-3">
                <label for="bio" class="form-label">Bio</label>
                <textarea name="bio" class="form-control" id="" cols="30" rows="10" style="white-space: pre-line;"><%= user.Profile?.bio %></textarea>
            </div>
            <div class="mb-3 form-check">
                <label for="hobby" class="form-label">hobby</label>
                <input name="hobby" type="text" class="form-control" id="hobby" value="<%= user.Profile?.hobby %>">
            </div>
            <button type="submit" class="btn btn-primary">Edit Profile</button>
        </form>
    </div>
</body>
</html>