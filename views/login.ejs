<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('css') %>
    <title>Login Form</title>
</head>
<body>
    <div class="container">
        <div class="row mt-5 p-5">
            <h1>Login Form</h1>
            <% if (errors) { %>
                <div class="row mb-2 mt-2 text-danger">
                    <%= errors %>
                </div>
            <% } %>
            <form action="/login" method="post">
                <div class="mb-3">
                    <label for="email" class="form-label">Email address</label>
                    <input name="email" type="email" class="form-control" placeholder="Type your email">
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input name="password" type="password" class="form-control" placeholder="Type your password">
                </div>
                <div class="row p-2">
                    <div class="col-auto">
                        <input class="btn btn-primary" type="submit" value="Submit">
                    </div>
                    <div class="col-auto">
                        <p>Belum punya akun? <a href="/register">Daftar</a> aja dulu</p>
                    </div>
                </div>
            </form>
        </div>
    </div>
</body>
</html>